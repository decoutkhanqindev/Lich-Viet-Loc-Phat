package com.decoutkhanqindev.lich_viet_loc_phat.theme

import androidx.compose.ui.graphics.Color

// Classic & Glass Liquid theme — Vietnamese lunar calendar
val BaTrauDark = Color(0xFF4A1010)      // Đỏ bã trầu đậm — top of background gradient
val BaTrauMid = Color(0xFF6B1A1A)       // Đỏ bã trầu giữa
val NauToi = Color(0xFF1A0A0A)          // Nâu tối — bottom of background gradient

val GoldAccent = Color(0xFFD4A840)      // Vàng đồng — active tab, auspicious hours
val GoldLight = Color(0xFFF0C860)       // Vàng nhạt — highlight
val IvoryWhite = Color(0xFFF5F0E8)      // Trắng ngà — text on dark background

val GlassTint = Color(0x26FFFFFF)       // Glass card tint (alpha 15%)
val GlassBorder = Color(0x33FFFFFF)     // Glass card border (alpha 20%)
val GlassBorderStrong = Color(0x66FFFFFF) // Selected day border (alpha 40%)

val TodayGlow = Color(0xFFD4A840)       // Hôm nay — gold glow circle
val HolidayDot = Color(0xFFFF6B6B)      // Dot chỉ thị lễ/rằm/mùng một
val WeekendColor = Color(0xFFFF8A80)    // T7/CN màu hồng đỏ

val AuspiciousGold = Color(0xFFD4A840)  // Giờ Hoàng Đạo
val InauspiciousGray = Color(0x80FFFFFF) // Giờ Hắc Đạo — mờ
